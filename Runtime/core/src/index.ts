import {binding, extension} from "./binding";
import {createElement} from "./createElement";

const Shiba = {
    binding,
    createElement,
    extension,
};

export default Shiba;

export * from "./binding";
