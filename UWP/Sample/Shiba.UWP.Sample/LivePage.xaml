<Page
    x:Class="Shiba.UWP.Sample.LivePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Shiba.UWP.Sample"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:shiba="using:Shiba"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBox Text="{x:Bind Streaming.Host,Mode=TwoWay}" PlaceholderText="Host" Grid.ColumnSpan="2" Margin="8" Padding="8"/>
        <Button Click="{x:Bind Streaming.Start}" Grid.Row="1" Content="Connect" Margin="8" HorizontalAlignment="Stretch" />
        <Button Click="{x:Bind Streaming.Stop}" Grid.Row="1" Grid.Column="1" Content="Disconnect" Margin="8" HorizontalAlignment="Stretch" />
        <shiba:ShibaHost Name="ShibaHost" Grid.ColumnSpan="2" Grid.Row="2" />
    </Grid>
</Page>
